# ⚔️ Тени Эльдории - Telegram WebApp (Всё-в-одном)

Этот проект объединяет Telegram-бота и веб-приложение (Mini App) в один сервис. Он идеально подходит для бесплатного деплоя на платформы типа Railway или Render, которые предоставляют автоматический HTTPS.

## 🚀 Как запустить (Самый простой способ)

### 1. Подготовка
1. Создайте репозиторий на GitHub и загрузите туда этот код.
2. Зарегистрируйтесь на [Railway.app](https://railway.app/) или [Render.com](https://render.com/).

### 2. Деплой
1. На хостинге выберите **"New Project"** -> **"Deploy from GitHub"**.
2. Выберите ваш репозиторий.
3. В разделе **Variables** (или Environment) добавьте:
   - `BOT_TOKEN` — токен от @BotFather.
   - `CHANNEL_ID` — ID канала для получения отзывов.
   - `WEBAPP_URL` — (см. ниже).

### 3. Настройка WEBAPP_URL
- **На Railway:** Вам **не нужно** ничего заполнять! Бот сам определит свой адрес через переменную `RAILWAY_PUBLIC_DOMAIN`.
- **На других сервисах:** Вставьте публичную HTTPS-ссылку в переменную `WEBAPP_URL`.

---

## 🐳 Локальный запуск (Docker)

Если вы хотите запустить проект на своем компьютере:

1. Создайте файл `.env` и заполните его:
   ```env
   BOT_TOKEN=ваш_токен
   CHANNEL_ID=ваш_id
   WEBAPP_URL=http://localhost:8080
   ```
2. Запустите:
   ```bash
   docker-compose up -d --build
   ```
3. Приложение будет доступно по адресу `http://localhost:8080`.

---

## 📁 Структура проекта

- `server.py` — **Единый центр**: запускает и бота, и веб-сервер.
- `public/` — Весь интерфейс Mini App (HTML/CSS/JS).
- `Dockerfile` — Инструкция для сборки контейнера.
- `requirements.txt` — Список библиотек Python.

## 🤖 Команды бота

- `/start` — Открыть главное меню с кнопкой запуска Mini App.
- `/help` — Справка.
- `/rules` — Краткие правила.

---

Сделано с 🖤 для любителей тёмного фэнтези
