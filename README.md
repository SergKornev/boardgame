# ⚔️ Тени Эльдории - Telegram WebApp

Веб-приложение для Telegram бота, посвящённое настольной RPG игре в стиле тёмного фэнтези.

## 🎮 Возможности

- 📜 **Описание игры** — погрузитесь в мир Эльдории
- ⚔️ **Правила** — полное руководство по игре
- ✉️ **Обратная связь** — отправка отзывов и предложений в канал

## 🚀 Деплой на бесплатный хостинг (Railway / Render)

Эти платформы предоставляют автоматический HTTPS и бесплатный уровень использования.

### 1. Подготовка
1. Создайте аккаунт на [Railway.app](https://railway.app/) или [Render.com](https://render.com/).
2. Создайте новый проект и подключите ваш GitHub репозиторий с этим кодом.

### 2. Настройка переменных окружения
В настройках проекта на хостинге добавьте следующие переменные (Environment Variables):
- `BOT_TOKEN` — токен вашего бота от @BotFather.
- `CHANNEL_ID` — ID канала для отзывов.
- `WEBAPP_URL` — URL, который выдаст вам хостинг (например, `https://boardgame-production.up.railway.app`).
- `PORT` — 3000 (или оставьте по умолчанию).

### 3. Запуск
Хостинг автоматически обнаружит `Dockerfile` и запустит приложение. После деплоя не забудьте обновить `WEBAPP_URL` в переменных окружения, если вы не знали его заранее.

---

## 🐳 Локальный запуск через Docker

### 1. Настройте .env
```bash
cp .env.example .env
```
Заполните `BOT_TOKEN`, `CHANNEL_ID` и `WEBAPP_URL`.

### 2. Запустите
```bash
docker-compose up -d --build
```

---

## 📁 Структура проекта

```
├── server.py           # Flask сервер + Telegram бот (aiogram)
├── requirements.txt    # Python зависимости
├── public/
│   ├── index.html      # Главная страница WebApp
│   ├── styles.css      # Стили в стиле тёмного фэнтези
│   └── script.js       # Клиентская логика
├── Dockerfile          # Docker образ
└── docker-compose.yml  # Docker Compose конфигурация
```

## 🤖 Команды бота

- `/start` — Открыть главное меню с WebApp
- `/help` — Показать справку
- `/rules` — Краткие правила игры

---

Сделано с 🖤 для любителей тёмного фэнтези
